# 逆序单链表.  
```
def reverseList(self, head):
        if head == None:
            return head 

        next_node = self.reverseList(head.next)

        if next_node != None:
            next_node.next = head

        head.next = None 
        return head
```

# 正序分组.
